import Image from "next/image";
import { Cards, Callout } from "nextra/components";
import { FaGithub } from "react-icons/fa";
import firmware_architecture from "./pics/firmware-architecture.png";
import firmware_architecture_2 from "./pics/firmware-architecture-2.png";

# K-Scale Operating System

<Callout type="warning" emoji="⚠️">
This page is currently under construction.
</Callout>

```bash
pip install pykos
```

The K-Scale Operating System (OS) combines the hardware, software and firmware to provide an open-source skeleton for powering general-purpose robots.

<Cards>
  <Cards.Card
    icon={<FaGithub />}
    title="Github"
    href="https://github.com/kscalelabs/kos"
    description="Contribute to our open-source projects"
  />
</Cards>

If you need to develop with KOS on your robot, clone the repository and follow the directions on the README.

Use protoc 3 (releases [here](https://github.com/protocolbuffers/protobuf/releases))

Here's an example script for downloading and installing protoc 3 on Jetsons:

```
# Create a temporary directory
mkdir -p /tmp/protoc
cd /tmp/protoc

# Download the zip file
wget https://github.com/protocolbuffers/protobuf/releases/download/v29.0/protoc-29.0-linux-aarch_64.zip

# Unzip the contents
unzip protoc-29.0-linux-aarch_64.zip

# Copy the protoc binary to a directory in your PATH
sudo cp bin/protoc /usr/local/bin/

# Copy the protobuf include files
sudo cp -r include/* /usr/local/include/

# Clean up
cd ..
rm -rf /tmp/protoc
```

Here is a high-level overview of the K-Scale OS architecture.

<Image
  alt="Firmware Architecture"
  src={firmware_architecture}
  priority
  style={{ maxWidth: "100%", height: "auto", marginTop: "2rem" }}
/>

Here is a different diagram of how the K-Scale OS is organized.

<Image
  alt="Firmware Architecture 2"
  src={firmware_architecture_2}
  priority
  style={{ maxWidth: "100%", height: "auto", marginTop: "2rem" }}
/>
